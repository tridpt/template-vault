# Port the server listens on
PORT=4000

# Optional admin token. Leave empty to keep the app fully open (local use).
# When set, creating/editing/deleting templates requires this token
# (sent as "Authorization: Bearer <token>"). Reads stay public.
ADMIN_TOKEN=
